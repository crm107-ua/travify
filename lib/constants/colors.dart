import 'package:flutter/material.dart';

class AppColors {
  // Colores predefinidos de Flutter
  static const Color primary = Color.fromARGB(255, 29, 123, 231);
  static const Color secondary = Colors.green;
  static const Color accent = Colors.orange;

  // Colores personalizados utilizando códigos hexadecimales
  static const Color customBlue = Color(0xFF1E88E5);
  static const Color customGray = Color(0xFF9E9E9E);
  static const Color customBlack = Color(0xFF000000);
}
